{% extends "template.njk" %}

{% set title = "Sorry, there is a problem with the prototype" %}
{% set stackHtml %}
<pre class="x-govuk-code x-govuk-code--block nhsuk-!-margin-0" tabindex="0"><code class="nhsuk-!-font-size-14">{{ error.stack }}</code></pre>
{% endset %}

{% block content %}
  <div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-two-thirds">
      <h1 class="nhsuk-heading-xl">{{ title }}</h1>

      <h2 class="nhsuk-heading-m nhsuk-!-margin-bottom-2">{{ error.name }}</h2>
      <p class="nhsuk-body">{{ error.message | nl2br | nl2br | safe }}</p>
      {{ details({
        summaryHtml: "Stack trace",
        html: stackHtml
      }) }}

      <pre class="nhsuk-!-margin-top-8">Status code: {{ status }}</pre>
    </div>
  </div>
{% endblock %}
